import React from "react";
import ReactDOM from "react-dom";
import ControlPanel from "../02example/ControlPanel";

const rootElement = document.getElementById("root");
ReactDOM.render(<ControlPanel />, rootElement);
